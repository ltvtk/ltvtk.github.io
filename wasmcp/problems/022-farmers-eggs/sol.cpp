#include <bits/stdc++.h>
using namespace std;

int main() {
    int X;
    cin >> X;
    int before3 = (X + 1) * 2;
    int before2 = (before3 + 1) * 2;
    int start   = (before2 + 1) * 2;
    cout << start << endl;
    return 0;
}
